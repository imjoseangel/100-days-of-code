dist: bionic
language: python
python:
  - "3.7"

cache:
  pip: true

install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python scripts/pylint_check.py
  - python scripts/radon_check.py
